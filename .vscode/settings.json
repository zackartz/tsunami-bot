{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "pog",
            "database": "${workspaceFolder:tsunami-bot}/dev.db"
        }
    ]
}